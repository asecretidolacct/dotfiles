- defaults:
    link:
      relink: true
      relative: true
      create: true
        
- clean: ['~', '~/.config']
  
- link: 
    ~/.config/git: git
  
- shell: 
    - [git submodule update --init --recursive, Installing submodules]
